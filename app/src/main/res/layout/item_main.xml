<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="movie"
            type="com.devforbest.movieapp.network.responses.ResponseMovie" />

        <variable
            name="handlers"
            type="com.devforbest.movieapp.views.HandlerEvent" />
    </data>
    <!-- A CardView that contains a TextView -->
    <android.support.v7.widget.CardView xmlns:card_view="http://schemas.android.com/apk/res-auto"
        android:id="@+id/card_view"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_gravity="center"
        android:layout_margin="16dp"
        android:focusable="true"
        card_view:cardCornerRadius="4dp"
        card_view:cardElevation="5dp">


        <ImageView
            android:id="@+id/back"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"

            android:onClick="@{(view  ) -> handlers.goDetail(view, movie.id)}" />

        <!--<ImageView-->
        <!--android:id="@+id/favorite"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:layout_gravity="right |bottom"-->
        <!--android:layout_margin="8dp"-->
        <!--android:src="@drawable/ic_favorite_border_black_48dp" />-->
        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/fab_margin"
            app:layout_anchor="@id/back"
            app:layout_anchorGravity="bottom|end"
            app:srcCompat="@drawable/ic_favorite_border_black_48dp" />

        <TextView
            android:id="@+id/info_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center|center_horizontal"
            android:gravity="center_vertical"
            android:shadowColor="@color/colorPrimaryDark"
            android:text="@{movie.title}"
            android:textAllCaps="true"
            android:textColor="@android:color/white"
            android:textSize="20sp" />

    </android.support.v7.widget.CardView>
</layout>
